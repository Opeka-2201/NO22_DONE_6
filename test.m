// matlab

// create a bridge to the matlab engine

// engOpenSingleUse(NULL, NULL, NULL);

// engEvalString(ep, "a = 1");

// engEvalString(ep, "b = 2");

// engEvalString(ep, "c = a + b");

// mxArray *result = engGetVariable(ep, "c");

